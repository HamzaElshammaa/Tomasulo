package model;

public class Source {
    public enum unit {
        A, //waiting for Add
        M, //waitng for Mult
        REG, //Register
        FP_REG, //floating point REG
    }
    public int index;
}
